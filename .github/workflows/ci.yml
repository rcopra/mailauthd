name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
      - run: go test ./...
      - run: go vet ./...
      - name: Fuzz smoke
        run: go test ./internal/spf/ -run '^$' -fuzz FuzzParseRecord -fuzztime 10s